stages:
  - validate
  - test

variables:
  COMPOSER_ALLOW_SUPERUSER: 1
  COMPOSER_CACHE_DIR: ".composer-cache"
  TYPO3_PATH_ROOT: "$CI_PROJECT_DIR/.Build/Web"

.php-grumphp:
  stage: test
  image: kanti/buildy:${PHP_VERSION}
  cache:
    key: composer-cache-${PHP_VERSION}
    paths:
      - .composer-cache/
  before_script:
    - composer install --no-progress --no-interaction
  script:
    - php ./.Build/bin/grumphp run

.php-unit-test:
  stage: test
  image: kanti/buildy:${PHP_VERSION}
  cache:
    key: composer-cache-${PHP_VERSION}
    paths:
      - .composer-cache/
  before_script:
    - composer install --no-progress --no-interaction
  script:
    - php ./.Build/bin/phpunit -c phpunit.xml

validate:composer:
  stage: validate
  image: composer:latest
  script:
    - composer validate --strict

php-grumphp-8.2:
  extends: .php-grumphp
  variables:
    PHP_VERSION: "8.2"

php-grumphp-8.3:
  extends: .php-grumphp
  variables:
    PHP_VERSION: "8.3"

php-grumphp-8.4:
  extends: .php-grumphp
  variables:
    PHP_VERSION: "8.4"

php-unit-test-8.2:
  extends: .php-unit-test
  variables:
    PHP_VERSION: "8.2"

php-unit-test-8.3:
  extends: .php-unit-test
  variables:
    PHP_VERSION: "8.3"

php-unit-test-8.4:
  extends: .php-unit-test
  variables:
    PHP_VERSION: "8.4"
